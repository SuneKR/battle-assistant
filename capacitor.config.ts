import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'battleAssistant',
  appName: 'battleAssistant',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
